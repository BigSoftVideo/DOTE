## Export transcript clips to CSV file <a id='export-CSV'></a>

This export function is specifically for exporting just the Transcript Clips in the current Transcript to a universal comma-separated version (CSV) format that can be read and parsed by other software tools.

### Exporting to CSV

1. Select ...